(ns {{ns-name}}.core-test
    (:require [cljs.test :refer-macros [deftest testing is]]
              [{{ns-name}}.core :refer :all]))

(deftest another-test
  (testing "FIXME, I fail too!"
    (is (= 1 2))))
